name: Análise Técnica Diária

on:
  schedule:
    # Roda de segunda a sexta, às 21:00 UTC (18:00 no horário de Brasília)
    - cron: '0 21 * * 1-5'
  workflow_dispatch: # Permite rodar manualmente pelo botão no GitHub

jobs:
  run-analise:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar o repositório
        uses: actions/checkout@v4

      - name: Configurar o Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar bibliotecas
        run: pip install yfinance pandas requests git+https://github.com/twopirllc/pandas-ta.git

      - name: Rodar o script de análise
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python analise_ticket.py
